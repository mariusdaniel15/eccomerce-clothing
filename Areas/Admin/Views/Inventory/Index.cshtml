@model List<ProductInventory>

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                INVENTORY LIST
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Inventory" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-person-fill-add"></i>  NEW INVENTORY
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>
                Quantity
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td>@obj.Quantity</td>
                <td>
                    <div class="d-flex justify-content-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Inventory" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i> EDIT
                            </a>
                            <a asp-controller="Inventory" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash"></i> DELETE
                            </a>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>